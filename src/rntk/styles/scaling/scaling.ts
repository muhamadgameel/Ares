// TODO temp placeholder functions
const scale = (value: number): number => value;

abstract class Scaling {
  // vertical spacing (heights)
  public static v = scale;

  // horizontal spacing (widths)
  public static h = scale;

  // font sizes
  public static f = scale;

  // generic spacing
  public static s = scale;
}

export default Scaling;
