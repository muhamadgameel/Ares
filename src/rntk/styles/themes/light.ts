import type { Theme } from './themes.types';
import lightColors from '../colors/light';
import typography from '../typography/default';
import spacing from '../spacing/default';

const lightTheme: Theme = {
  colors: lightColors,
  typography,
  spacing,
};

export default lightTheme;
